log2es {
    configuration {
        hosts = ["127.0.0.1:9300"]

    }

    loggers = ["akka.event.Logging$DefaultLogger"]
    loglevel = "DEBUG"

    debug {
        lifecycle = on
        autoreceive = on
        receive = on
        fsm = on
        event-stream = on
    }
}