log2es {
    configuration {
        hosts = ["http://localhost:9200"]
    }

    loggers = ["akka.event.Logging$DefaultLogger"]
    loglevel = "DEBUG"

    debug {
        lifecycle = on
        autoreceive = on
        receive = on
        fsm = on
        event-stream = on
    }
}